rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Auth is handled by NextAuth — open access for now.
      // TODO: lock down with Firebase Admin SDK custom tokens.
      allow read, write: if true;
    }
  }
}
